name: flutter_adapt_kit
description: "Zero-code-invasion screen adaptation plugin for Flutter. Supports scaling, safe area, and text adaptation."
version: 0.1.0
homepage: https://github.com/your-org/flutter_adapt_kit

environment:
  sdk: ^3.0.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
